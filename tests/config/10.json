{
    "binary": 0,
    "timeout": 100,
    "seed": 10000010,
    "params": {
        "max_size": 1900,
        "expr": "A*B*C*D-E+F+G+H-I+J"
    },
    "description": "complex matrix operations (smaller than ${matrixsize} * ${matrixsize}, ${timeout} sec timeout)",
    "preparation": "cat ${filename} > /dev/null"
}
